{
  "name": "@gitgud/ai-agent-service",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8001",
    "install-deps": "pip install -r requirements.txt",
    "build": "echo 'No build required for python'",
    "check": "echo 'No checks'"
  }
}
